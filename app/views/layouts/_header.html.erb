<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
        
         <a class="navbar-brand" href="<%= root_path %>">
            <%= image_tag "logo_sm_w.png", title:"logo", style: "max-height: 40px; margin-top: -10px;"%>
         </a>

      <a class="navbar-brand" href="<%= root_path %>">Una Gauchada</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <!--
      <ul class="nav navbar-nav">
        <li><a href="#">Link</a></li>
      </ul>

      <form class="navbar-form navbar-left">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>
      -->
    
      <ul class="nav navbar-nav navbar-right">
        <% if usuario_signed_in? %>
        <% ape = current_usuario.apellido.capitalize %> 
        <% nom = current_usuario.nombre.capitalize %> 
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" style="overflow: auto">
                <div class="image-container-nav" style="margin-top: -15px; margin-bottom: -15px; float: right;">
                    <% if current_usuario.foto.presence %>
                    <%= image_tag current_usuario.foto, title:"FotoUsuario", class:"img-sm"%>
                    <% else %>
                    <%= image_tag "foto_stock.png", title:"FotoUsuario", class:"img-sm"%>
                    <% end %>
                </div> 
                <div style="float: left; margin-left: -13px">

                    <span class="caret" style="margin-bottom: -40px; margin-right: 4px;"></span>
                </div>
                
          </a>
          <ul class="dropdown-menu">
            <li><a href="<%= usuario_path(:id => current_usuario.id) %>">Mi perfil</a></li>
            <li><a href="<%= edit_usuario_registration_path %>">Editar mi perfil</a></li>
            <% if usuario_es_admin? %>
            <li role="separator" class="divider"></li>
            <li><a href="<%= logros_path %>"><strong> Listado de Logros </strong></a></li>
            <% end %>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Cerrar Sesion", destroy_usuario_session_path, :method => :delete %></li>
          </ul>
        </li>
        <% else %>
        <ul class="nav navbar-nav">
          <li><a href="<%= new_usuario_session_path %>">Iniciar Sesión</a></li>
          <li><a href="<%= new_usuario_registration_path %>">Registrate</a></li>
        </ul>
        <% end %>

      </ul>


    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>